# Database Backup Configuration
# Copy this file to .env and configure your backup settings

# Backup target directory (REQUIRED - backup will not run without this)
# Example: /path/to/your/backup/folder
BACKUP_TARGET_DIR=

# Source database file to backup (optional, defaults to ./strapi/database/data.db)
# BACKUP_SOURCE_FILE=./strapi/database/data.db

# Number of days to keep backups (optional, defaults to 2)
# BACKUP_RETENTION_DAYS=2

# Polling interval in milliseconds (optional, defaults to 30000 = 30 seconds)
# BACKUP_POLL_INTERVAL=30000
